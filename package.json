{
  "name": "personal-portfolio-site",
  "version": "1.0.0",
  "description": "Personal portfolio website for AI/ML engineer and researcher",
  "main": "index.html",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:pbt": "vitest --run tests/property-tests",
    "build": "node build-optimize.js",
    "perf": "node performance-test.js",
    "optimize": "npm run build && npm run perf",
    "verify-deployment": "node verify-deployment.js",
    "deploy-check": "npm run test && npm run verify-deployment"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0",
    "fast-check": "^3.15.0"
  },
  "type": "module"
}