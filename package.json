{
  "name": "personal-portfolio-site",
  "version": "1.0.0",
  "description": "Personal portfolio website for AI/ML engineer and researcher",
  "main": "index.html",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:pbt": "vitest --run tests/property-tests",
    "build": "node scripts/build-optimize.cjs",
    "build-beer": "node scripts/build-beer.cjs",
    "build-all": "npm run build-beer && npm run build",
    "perf": "node tests/performance-test.js",
    "optimize": "npm run build && npm run perf",
    "verify-deployment": "node tests/verify-deployment.js",
    "deploy-check": "npm run test && npm run verify-deployment"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0",
    "fast-check": "^3.15.0"
  },
  "type": "module"
}